{
  "homepage": "http://zouhourball.github.io/Chart",
  "version": "0.0.1",
  "scripts": {
    "start": "cd frontend && npm run dev",
    "predeploy": "cd frontend && npm run build",
    "deploy": "gh-pages -d build",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
